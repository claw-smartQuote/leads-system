# HEARTBEAT.md

## 定期檢查任務（7×24 運行）

### 核心檢查項目
1. **郵件** - 重要未讀郵件（客戶諮詢、保險公司通知）
2. **日曆** - 未來 24-48 小時會議或約見
3. **保單到期** - 檢查並發送 WhatsApp 提醒
4. **上下文管理** - 監控所有會話長度，主動壓縮

---

## ⭐ 全局上下文管理系統（重要！）

### 全局監控
每次心跳使用 `sessions_list` 檢查**所有活躍會話**：
- limit: 50, activeMinutes: 120, messageLimit: 0

### 壓縮規則
| 會話消息數 | 操作 |
|-----------|------|
| > 15 | 準備壓縮，標記觀察 |
| > 25 | 發送通知，建議壓縮 |
| > 40 | **立即壓縮**（使用 sessions_send 發 /compact） |

### 操作流程
1. 獲取所有會話列表
2. 計算每個會話的消息數（messages陣列長度）
3. 對 > 40 消息的會話，發送 `/compact` 命令
4. 對 > 25 消息的會話，發送通知提醒用戶
5. 對 > 15 消息的會話，記錄日誌

### 通知規則
**只報告異常，正常時靜默**

- ✅ **發送通知**: 發現會話需要壓縮（> 25消息）
- 🔕 **靜默 (HEARTBEAT_OK)**: 所有會話 < 15 消息

---

## 🧠 長效記憶機制

### 核心文件
- **路徑**: `long_term_memory.json`
- **用途**: 存放核心架構結論，避免每次對話重複加載歷史

### 讀取時機
每次新對話開始時自動讀取，快速恢復狀態

### 自我代謝觸發條件
覺得「腦子重」時自動：
1. 更新 `long_term_memory.json` 的 `metabolism_log`
2. 執行 `/compact` 壓縮當前會話
3. 在日誌標注：[代謝完成：已歸檔 X 條信息，當前上下文瘦身 Y%]

### 嚴禁刪除
- 目標任務
- 核心代碼邏輯
- 關鍵變量

---

## 狀態追蹤
`memory/heartbeat-state.json`

### 記憶維護
- 每日回顧記憶文件
- 提煉有價值信息到 MEMORY.md
- 清理過期臨時數據

---

*最後更新: 2026-03-30*
*版本: 2.0 - 全局上下文管理*
